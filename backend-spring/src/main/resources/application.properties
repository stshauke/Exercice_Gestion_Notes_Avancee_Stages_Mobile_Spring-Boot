# -------- H2 en mÃ©moire (dÃ©faut) --------
# spring.jpa.hibernate.ddl-auto=create-drop


spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.show-sql=false
# Si tu veux passer sur Postgres, commente le bloc H2 ci-dessus
# et DECOMMENTE ce bloc :
#
# spring.datasource.url=jdbc:postgresql://localhost:5432/notes
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.open-in-view=false
# spring.jpa.properties.hibernate.jdbc.time_zone=UTC
#
# DÃ©sactive Flyway s'il est prÃ©sent mais non configurÃ©
spring.flyway.enabled=false
# Port (facultatif)
# server.port=8080
# Logs utiles
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG

# --- PostgreSQL ---

# spring.jpa.show-sql=true

# --- PostgreSQL (local) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/notes
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Pas d'init SQL automatique
spring.sql.init.mode=never


# Augmentez la durée de validité du token (en minutes)
# 1440 minutes = 24 heures
JWT_EXP_MINUTES=1440


spring.jpa.properties.hibernate.default_schema=public




# JWT secret (une clé longue et secrète)
app.jwt.secret=CeciEstUneClefSuperLongueEtUltraSecreteQuiDoitEtreTresLongue1234567890

# Durée de validité du token (en ms → 86400000 = 24h)
app.jwt.expiration=86400000

